-- 대소문자만 다른 계정이 생기지 않게 한다.
--
-- users.email의 UNIQUE는 대소문자를 구분하는데, 공개 지원 폼은 이메일을 항상
-- 소문자로 저장한다. 그래서 이메일에 대문자가 한 글자만 있어도 같은 사람이 두
-- 계정으로 갈라졌다 — 서류합격 처리가 기존 계정을 못 찾아 새 계정을 만들고,
-- 원래 계정으로 로그인한 지원자는 자기 면접방에서 403으로 막혔다. 반대로
-- 대소문자만 바꿔 가입하면 남이 낸 지원 내역과 접수번호가 "내 지원 현황"에
-- 그대로 보였다.
--
-- 애플리케이션이 저장·조회 전에 소문자로 통일하도록 고쳤고, 여기서 DB 차원의
-- 제약을 더해 우회 경로를 남기지 않는다.

-- 기존 행을 같은 규칙으로 정리한다 (이미 소문자면 바뀌는 것이 없다).
UPDATE users SET email = LOWER(TRIM(email)) WHERE email <> LOWER(TRIM(email));

-- 이제부터는 표기가 달라도 같은 이메일로 두 계정을 만들 수 없다.
CREATE UNIQUE INDEX idx_users_email_lower ON users(LOWER(email));
