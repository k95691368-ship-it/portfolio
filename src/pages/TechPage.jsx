import { useState } from 'react'
import { Link } from 'react-router-dom'

// 기술 구현 상세.
//
// 화면만 보면 "무엇을 만들었는지"는 보이지만 "어떻게 만들었는지"는 보이지 않고,
// 코드만 보면 그 반대다. 두 갈래로 나눠 각각 끝까지 설명한다.

const SYSTEM = [
  {
    step: '01',
    title: '채용 공고와 공개 지원',
    body: '공고에 근로조건을 자유 텍스트가 아니라 구조화된 값(임금 범위·근무시간·근무일)으로 담습니다. 지원자는 로그인 없이 지원하고 접수번호를 받아 그 번호로 현황을 조회합니다. 나중에 계약서와 공고를 값으로 대조하기 위한 준비이기도 합니다.',
    detail: '공고 마감은 한국 달력 기준으로 판정합니다.',
  },
  {
    step: '02',
    title: '서류 심사와 지원자 비교',
    body: '지원서를 AI가 요약·평가하고, 여러 지원자를 같은 기준으로 나란히 놓고 비교합니다. 서류 합격 처리를 하면 그 이메일로 계정과 면접방이 자동으로 만들어집니다.',
    detail: 'AI 판정은 참고용이고, 합격 여부의 결정은 사람이 누릅니다.',
  },
  {
    step: '03',
    title: '면접방 대화와 조건 추출',
    body: '회사와 지원자가 한 방에서 대화합니다. AI가 대화에서 합의된 근로조건(임금·근무시간·근무일·개시일)을 뽑아 계약서 초안에 채웁니다.',
    detail: '뽑아낸 값은 나중에 "합의한 것과 계약서가 다른가"를 판정하는 기준이 됩니다.',
  },
  {
    step: '04',
    title: '전자근로계약서',
    body: '표준근로계약서 형식으로 조건을 작성합니다. 임금은 기본급 하나가 아니라 구성항목(기본급·고정수당·상여금·복리후생비·고정연장수당)으로 나눠 담습니다.',
    detail: '근로기준법 제17조가 요구하는 것이 "임금"이 아니라 임금의 구성항목·계산방법·지급방법이기 때문입니다.',
  },
  {
    step: '05',
    title: '서명 전 법령 점검',
    body: '최저임금(산입범위 반영)·주 52시간·제17조 명시사항·기간제 2년 상한·수습 감액 한도·공고와의 대조를 한 화면에서 계산해 보여 줍니다.',
    detail: '경고에 그치지 않습니다. 위법한 계약은 서명 요청 자체를 서버가 거부합니다. 화면을 우회해 직접 요청을 보내도 막힙니다.',
    accent: true,
  },
  {
    step: '06',
    title: '근로자 권리 계산',
    body: '"근로기준법에 따름"이라고만 적힌 항목을 실제 날짜와 금액으로 풉니다. 연차가 언제 며칠 생기는지, 퇴직금 대상인지, 연장·야간·휴일근로 한 시간에 얼마인지.',
    detail: '통상시급은 확실한 항목만으로 계산해 하한을 내고, 더 높아질 수 있다는 사실을 함께 밝힙니다.',
  },
  {
    step: '07',
    title: '전자서명과 무결성',
    body: '서명하는 순간의 계약 내용을 지문(SHA-256)으로 만들어 서명과 함께 저장합니다. 이후 내용이 바뀌면 그 서명은 바뀌기 전 내용에 대한 동의이므로 자동으로 무효화됩니다.',
    detail: '서명 그림만 남기면 "무엇에 동의했는가"를 특정할 수 없습니다. 마우스를 쓸 수 없는 사람을 위해 이름을 입력해 서명하는 경로도 함께 둡니다.',
  },
  {
    step: '08',
    title: '교부와 보존',
    body: '근로자가 계약서를 받을 수 있게 된 사실과 실제로 열어 본 시각을 남깁니다. 보존 기간 3년은 계약 종료일이 아니라 근로관계가 실제로 끝난 날부터 셉니다.',
    detail: '보존 의무가 남은 계약서는 관리자도 확인 없이 지울 수 없습니다.',
  },
  {
    step: '09',
    title: '감사추적증명서',
    body: '계약 이력을 계약서에서 떼어내 발급번호와 지문이 붙은 독립 문서로 만듭니다. 발급 시점의 사실을 그대로 동결하므로 이후 계약이 바뀌어도 발급된 증명서는 그대로 검증됩니다.',
    detail: '근로조건은 담기지 않습니다. 발급번호를 아는 것만으로 남의 임금이 보여서는 안 되기 때문입니다.',
    accent: true,
  },
  {
    step: '10',
    title: '로그인 없는 진위 확인',
    body: '증명서를 제시받은 사람(근로감독관·법률 상담·은행)이 발급번호만으로 진위를 확인합니다. 정본 문자열을 내려받아 자기 도구로 SHA-256을 계산해 대조할 수도 있습니다.',
    detail: '계정을 만들어야만 확인되는 증명서는 증명서로서 쓸모가 없습니다.',
  },
]

const STACK = [
  {
    area: '런타임 · 아키텍처',
    items: [
      ['Cloudflare Workers (V8 isolate)', 'Node 런타임이 없는 엣지 환경. crypto.subtle·fetch·TextEncoder 등 Web Standards API만으로 구현했습니다.'],
      ['파일 기반 라우팅', 'functions/api/rooms/[roomId]/sign.js 경로가 그대로 엔드포인트가 됩니다. 같은 이름의 파일과 디렉터리가 공존하면 /api/* 전체가 404가 되는 함정이 있어, 커밋 전 충돌 검사를 돌립니다.'],
      ['미들웨어 체인', '세션 검증으로 data.user를 주입하고, /api/admin/* 하위에 별도의 관리자 게이트를 겁니다.'],
      ['React 19 + Vite', 'SPA. 라우트별 코드 분할.'],
    ],
  },
  {
    area: '데이터 계층 (D1 / SQLite)',
    items: [
      ['마이그레이션 41개', '스키마 진화를 파일로 남겨 로컬과 원격에 같은 순서로 적용합니다.'],
      ['claim-then-check', 'D1에는 트랜잭션이 없습니다. ON CONFLICT ... WHERE 로 조건부 선점하고 meta.changes 로 성공 여부를 판정해, 중복 발송·중복 확정을 SQL 한 문장으로 막습니다.'],
      ['외래키 제약 대응', 'D1은 외래키를 강제합니다. 삭제는 batch로 참조하는 쪽부터 끊습니다. 이 순서를 놓쳐 감사 로그가 방을 참조한 채 남아 초기화가 영구히 실패하는 버그를 만들었고, 재현·수정·재확인까지 마쳤습니다.'],
      ['재귀 CTE', 'WITH RECURSIVE 로 갱신 계약 사슬을 거슬러 올라가 계속근로기간을 합산합니다. 깊이 상한을 두고, 거기서 끊겼으면 그 사실을 감추지 않고 함께 알립니다.'],
      ['부분 유니크 인덱스', '"한 공고에 심사 대기 지원서는 이메일당 하나"를 DB 레벨에서 보장합니다.'],
      ['인덱스 설계', '방 단위로 조회되는 표에 인덱스를 두고, 계약서 화면이 매번 도는 조회의 전체 스캔을 없앴습니다.'],
      ['표시용과 계산용의 분리', '수정 이력에 LIMIT을 두었더니 증명서가 그 뒤의 사건을 빠뜨린 채 해시됐고, 계약서 화면은 폐기된 값을 "합의값"으로 읽어 서명을 잠갔습니다. 계산은 전량으로, 화면 표시만 잘라내고 잘렸다는 사실을 함께 밝힙니다.'],
      ['최신부터 자르기', 'ORDER BY id ASC LIMIT 200은 대화의 맨 앞을 준다는 뜻입니다. 최신부터 잘라 다시 뒤집습니다.'],
    ],
  },
  {
    area: '암호 · 무결성',
    items: [
      ['PBKDF2-SHA256 10만 회', '비밀번호 해싱. 솔트는 계정마다 따로 생성합니다.'],
      ['세션 토큰 해시 저장', '평문 토큰을 저장하지 않고 SHA-256 해시만 둡니다. DB가 통째로 유출돼도 세션을 복원할 수 없습니다.'],
      ['정본 직렬화 (canonical serialization)', '키 순서를 코드로 고정하고 배열을 정렬해, 같은 내용이면 언제나 같은 문자열이 나오게 합니다. 그 문자열의 SHA-256이 문서 지문입니다.'],
      ['하위 호환 직렬화기', '증명서는 손으로 쓴 섹션 목록이 아니라 키 사전순 일반 직렬화기로 만듭니다. 나중에 필드가 늘어도 기존 줄의 출력이 그대로라, 옛 증명서가 변조로 뒤집히지 않습니다.'],
      ['재해시 검증', '검증은 발급 시 동결한 원본을 다시 해시해 대조합니다. 정본 문자열을 파일로 내려주어, 받는 사람이 자기 도구로 확인할 수 있습니다.'],
    ],
  },
  {
    area: 'LLM 연동',
    items: [
      ['SDK 없이 raw fetch', '번들 크기와 의존성 때문에 Anthropic API를 직접 호출합니다.'],
      ['tool_choice 강제 도구 호출', '자연어 응답을 파싱하지 않고, 스키마가 보장된 JSON만 받습니다.'],
      ['결정론과 비결정론의 분리', '맥락 판단이 필요한 서류심사·계약서 작성·번역은 LLM에, 최저임금·근로시간·연차 같은 법정 계산은 순수 함수에 둡니다. 재현되지 않는 판정은 근거가 될 수 없기 때문입니다.'],
      ['레이트리밋 티켓 반환', '호출이 실패하면 소모한 쿨다운을 되돌려, 실패한 시도가 다음 시도를 막지 않게 합니다.'],
    ],
  },
  {
    area: '프론트엔드',
    items: [
      ['React.lazy + 동적 import', 'PDF 라이브러리 약 600KB는 내보내기를 누른 순간에 불러옵니다. 계약서를 읽기만 하는 사람은 받지 않습니다.'],
      ['적응형 폴링', '대화가 오가면 2.5초, 조용해지면 최대 15초까지 백오프하고, 탭이 가려지면 요청을 멈춥니다.'],
      ['커서와 낙관적 갱신의 분리', '수신 커서는 폴링이 실제로 받아 온 것만 반영합니다. 내 메시지 id로 커서를 앞당기면 그 사이 상대가 보낸 메시지를 영영 건너뜁니다.'],
      ['집약 엔드포인트', '계약서 화면에 필요한 모든 데이터를 한 번의 요청으로 내려줍니다. 예전에는 한 페이지에 DB 조회가 스무 번 넘게 났습니다.'],
      ['입력 수단에 기대지 않기', 'canvas 는 Tab 순서에 들어가지 않아, 손으로 그리는 것만 되면 마우스를 쓸 수 없는 사람은 계약을 체결할 수 없습니다. 이름을 입력하면 캔버스에 그려 서명으로 삼는 경로를 함께 뒀습니다.'],
    ],
  },
  {
    area: '시간대',
    items: [
      ['UTC ↔ KST 경계 정규화', '서버는 UTC, 사용자는 한국(UTC+9)입니다. 한국시간 0~9시 아홉 시간 동안 서버의 "오늘"은 사용자의 "어제"라, 그동안 오늘 날짜가 미래로 거부되고 만료일 계산이 하루씩 밀렸습니다.'],
      ['계층별 처리', '날짜만 비교하는 자리는 한국 달력 기준으로, SQL은 date(\'now\', \'+9 hours\')로, 표시용 타임스탬프는 별도 포매터로 변환합니다. 순서만 필요한 감사추적은 UTC 그대로 둡니다.'],
      ['절반만 고치면 절반은 남는다', '서버의 날짜 비교를 먼저 고쳤는데 공고 마감 판정 세 곳, 프론트 계산, 서명 시각 표시가 그대로 남아 있었습니다. 같은 결함은 한 곳이 아니라 계층마다 있습니다.'],
    ],
  },
  {
    area: '테스트 · 운영',
    items: [
      ['3계층 테스트', '단위(오프라인 순수 함수) · 스모크(배포 환경, 읽기 전용) · 프로덕션 검증(쓰기 + 자체 정리).'],
      ['라우트 생존 검사', '라우트가 사라지면 SPA HTML이 200으로 돌아옵니다. 그래서 상태 코드보다 먼저 Content-Type이 JSON인지 검사합니다.'],
      ['서버 코드 빌드 체인', 'vite build는 프론트만 봅니다. 서버 코드의 import 오류가 배포 후에야 드러나 실제로 배포가 통째로 실패한 적이 있어, 빌드에 Functions 번들링을 붙였습니다.'],
      ['배포 상태 검증과 전파 대기', '배포 목록의 상태를 직접 조회해 실패를 실패로 알아봅니다. 성공 직후에는 새 워커와 옛 워커가 섞여 잡히므로 전파를 기다린 뒤 검증합니다.'],
      ['죽은 코드 정리', '호출부가 0곳인 export를 주기적으로 걷어냅니다. 다만 "안 쓰인다"에도 종류가 있어, 대체된 것은 지우고 아직 연결되지 않은 것은 연결합니다. 테스트만 부르는 함수는 살아 있는 것이 아니므로 함수와 테스트를 함께 지웁니다.'],
      ['내부 함수는 닫아 둔다', '테스트를 위해 export를 열면 쓰지도 않는 공개 표면이 남습니다. 내부 구현은 닫고, 공개 경로를 통해 검사합니다.'],
      ['다중 에이전트 결함 탐색', '접근성·시간대·대용량·반쯤 만든 기능 같은 관점을 나눠 병렬로 훑고, 찾은 것마다 반증을 시도해 걸러냅니다. 27개 에이전트가 낸 16건 중 반증을 통과한 것만 고칩니다.'],
    ],
  },
]

export default function TechPage() {
  const [tab, setTab] = useState('system')

  return (
    <div className="tech-page">
      <header className="page-header">
        <Link to="/" className="back-link">
          ← 첫 화면
        </Link>
        <h1>기술 구현</h1>
        <p>무엇을 만들었는지와 어떻게 만들었는지를 나눠 정리했습니다.</p>
      </header>

      <div className="tech-tabs" role="tablist" aria-label="설명 갈래">
        <button
          type="button"
          role="tab"
          aria-selected={tab === 'system'}
          className={`tech-tab${tab === 'system' ? ' active' : ''}`}
          onClick={() => setTab('system')}
        >
          <span className="tech-tab-title">무엇을 만들었나</span>
          <span className="tech-tab-desc">채용 공고부터 증명서 검증까지 10단계</span>
        </button>
        <button
          type="button"
          role="tab"
          aria-selected={tab === 'stack'}
          className={`tech-tab${tab === 'stack' ? ' active' : ''}`}
          onClick={() => setTab('stack')}
        >
          <span className="tech-tab-title">어떻게 만들었나</span>
          <span className="tech-tab-desc">런타임·데이터·암호·LLM·프론트·운영</span>
        </button>
      </div>

      {tab === 'system' ? (
        <section className="tech-panel">
          <p className="tech-lead">
            흩어져 있던 채용과 근로계약 과정을 하나로 이었습니다. 각 단계는 앞 단계가 남긴 값을 근거로
            다음 판단을 합니다.
          </p>
          <ol className="tech-steps">
            {SYSTEM.map((s) => (
              <li key={s.step} className={s.accent ? 'accent' : ''}>
                <span className="tech-step-num">{s.step}</span>
                <div className="tech-step-body">
                  <h3>{s.title}</h3>
                  <p>{s.body}</p>
                  {s.detail && <p className="tech-step-detail">{s.detail}</p>}
                </div>
              </li>
            ))}
          </ol>
        </section>
      ) : (
        <section className="tech-panel">
          <p className="tech-lead">
            서버리스 엣지 환경의 제약(트랜잭션 없음, Node API 없음)에서 무결성과 동시성을 어떻게 확보했는지가
            이 프로젝트의 기술적 중심입니다.
          </p>
          {STACK.map((group) => (
            <div className="tech-group" key={group.area}>
              <h3>{group.area}</h3>
              <dl className="tech-list">
                {group.items.map(([name, desc]) => (
                  <div className="tech-item" key={name}>
                    <dt>{name}</dt>
                    <dd>{desc}</dd>
                  </div>
                ))}
              </dl>
            </div>
          ))}
        </section>
      )}

      <p className="tech-foot">
        직접 눌러 보시려면 <Link to="/">첫 화면의 3분 체험</Link>을, 코드를 보시려면{' '}
        <a href="https://github.com/k95691368-ship-it/portfolio" target="_blank" rel="noreferrer">
          GitHub 저장소
        </a>
        를 확인해주세요.
      </p>
    </div>
  )
}
