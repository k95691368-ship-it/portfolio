# 개선 작업 기록

## 1회차 — 필수 개선 (2026-09-19, 로컬 반영)

직전 회차는 진행으로 분류했다. 전체 배치와 계정·지원 관리 기능이 변경됐고 로컬 테스트 116파일/1,388개, 빌드가 통과했다. 다만 화면 검증 서버는 종료됐으며 운영 서비스와 분리된 지속 로컬 실행은 미완료였다.

현재 기준선: `npm run dev`의 `/api`는 운영 Supabase로 프록시된다. 5173·5189·4173번 포트에 실행 서버가 없고 이 기록 파일도 없었다. 목표는 운영 변경이 아닌 로컬 빌드·실행·실제 동작 확인이므로 운영 연결을 대신 사용하는 것은 범위에 맞지 않는다.

선정 항목은 2개다. 첫째, 실제 서버 라우트와 PostgreSQL 호환 로컬 DB·첨부 저장소·외부 발송 없는 메일함을 연결한 로컬 실행을 만든다. 단순 정적 미리보기/목업은 저장·권한·메일 링크 흐름을 증명하지 못하므로 제외했다. 둘째, 가입 인증→로그인→공고→지원→접수 복구/수정/철회 흐름을 HTTP 및 실제 화면에서 확인하고 발견한 회귀를 수정한다. 브라우저만으로 반복하거나 운영 API를 호출하는 검증은 제외했다.

검증 계획: 실제 마이그레이션 적용/재시작/롤백, 경로 탈출·다른 출처·외부 요청 차단, 업로드 다운로드 바이트 일치, 계정/공고/지원 흐름의 실제 라우트 실행, 브라우저 조작, 전체 회귀 테스트·린트·로컬 빌드·접속 확인. 운영 메일 도착·외부 AI·외부 장치 통화는 로컬 검증으로 완료 처리하지 않는다.

진행: 실제 마이그레이션을 적용하는 PGlite 어댑터, 파일 저장소, 로컬 메일함, 루프백 전용 HTTP 서버, 외부 요청을 차단하는 로컬 빌드를 구현했다. 기본 dev/preview는 운영 프록시 대신 이 로컬 서버를 실행한다. 운영 데이터·시크릿은 읽지 않는다. 중복 실행 방지는 Windows named pipe를 실제 열어 배타성을 확인했다.

중간 검증: `npm run build:local` 성공(프런트 385모듈/API 101라우트). `npx vitest run tests/localRuntime.test.js tests/localLock.test.js tests/localDatabase.test.js tests/localStorage.test.js` 4파일/25개 통과(7.29초). 실제 HTTP 가입→로컬 메일 확인→로그인 유지→비밀번호 복구·기존 세션 해제, 공고 생성→PDF 제출·동일 바이트 다운로드→접수 복구→메일 소유 증명→수정→철회, 재시작 후 보존, 다른 출처·Host·경로 접근 차단을 실행했다. 초기 테스트 기대값 2개(복구 요청 202, 비로그인 /me 200+user:null)는 기존 계약에 맞춰 수정했다. 이 수치는 테스트 실행 시간이며 업무 시간 절약 수치가 아니다.

추가 변경: 실제 계약서 보관에서 사용되는 `text/html; charset=utf-8` MIME 매개변수를 로컬 저장소가 수용하도록 수정했다. 녹화 파일 부분 읽기는 호출부의 중첩 `range` 형식과 기존 평면 형식을 모두 검증하고, Supabase 어댑터는 공급자의 206·Content-Range·길이를 확인한다. 모의 공급자 테스트로 확인했으며 운영 저장소에는 반영하지 않았다. 오프라인 HTML의 외부 preconnect/DNS 힌트를 제거하고 미등록 HEAD 경로의 자체 fetch를 404 응답으로 바꿨다.

추가 검증: 실제 브라우저에서 체험 계정→공고 임시저장→게시→PDF 지원→접수번호→로컬 메일 소유 확인→지원서 수정을 실행했다. 1,440px 채용 관리 2열과 390px 지원 수정 1열을 확인했고 모바일 문서 너비 375px로 가로 넘침이 없었다. 모바일 메뉴 열기/닫기는 확인했다. 지원 철회 확인창 호출 시 브라우저 제어 응답이 멈춰 해당 화면 검증은 완료하지 못하고 임시 탭을 닫았다. 철회·오래된 수정 거부·재시작 보존은 실제 HTTP 테스트로 확인했다.

회귀 결과(14:12): `npm test` 120파일/1,438개 통과, 26.32초. 기본 샌드박스에서는 Vite 하위 프로세스가 EPERM으로 차단되어 승인된 로컬 테스트 권한으로 실행했다. `npm run lint` 오류 0, 기존 Fast Refresh 경고 3개. 계약서 보관은 실제 보관 함수와 PostgreSQL/파일 저장소를 연결해 실행했다. 시간은 검증 실행 시간이며 업무 절약 수치가 아니다.

로컬 반영: `npm run build` 성공(385모듈·API 101라우트·보관 작업 번들). `npm run local`로 최신 오프라인 빌드와 서버를 실행했다. `http://127.0.0.1:5173` HTTP 200, `/__local/health` 실제 라우트 101개·마이그레이션 8개 적용/원격 예약 1개 제외, `/api/jobs` 빈 목록을 확인했다. 새 브라우저에서 홈→공고와 모바일 메뉴→지원 현황 이동을 실행했다. 390px에서 문서 너비 390px, 이동 후 메뉴 닫힘을 확인했다. 외부 네트워크 힌트는 없고 connect-src는 self다.

정리: 5189 검증 서버를 종료하고 임시 DB·파일·메일 디렉터리, 검증 PDF·서버 헬퍼 및 배포용 임시 번들을 제거했다. 5173 서버의 빈 로컬 데이터는 유지했다. viewport 임시 변경은 원복했다. 삭제한 것은 이번 작업의 일회성 검증 데이터이며 운영 데이터는 삭제하지 않았다.

미검증: 브라우저 지원 철회 확인창 이후, 운영 메일·외부 AI·장치 통화·실제 Supabase 부분 읽기. 현재 한도 조회상 일반 사용 가능, 주간 잔여 49%; 초기화 크레딧은 사용하지 않았다.

다음: 구현→격리 검증→로컬 서버 실행 유지→검증용 데이터 정리→후속 필수 개선 점검. 푸시·클라우드 배포·운영 DB 변경은 범위 밖이다.

## 2회차 — 필수 개선 (2026-09-19, 로컬 반영)

기준선: 일회성 5189 서버에 가상 공고/지원서를 만들고, 지원서 이름 입력→왼쪽 지원 현황 메뉴→브라우저 뒤로 가기를 실행했다. 확인창 없이 이동되었고 돌아온 이름 입력은 0자였다. ApplyPage의 이탈 확인은 자체 뒤로 링크와 탭 종료에만 걸려 있으며 RecruitPage·ApplicationManagePage·ContractPage에는 페이지 이탈 보호가 없다.

선정은 최대 5개다. (1) 작성/수정 화면의 미저장 입력 이탈 보호(위험 4·영향 4·노력 3, 우선 24), (2) 지원서 저장 오류에서 입력과 첨부 재시도 상태의 정확성(4·4·2, 우선 32), (3) 긴 공고 제목의 줄바꿈, (4) 반폭 임금카드 입력 공간, (5) 모바일 메뉴 이동 후 키보드 초점. 3~5번은 코드 기반 후보이며 실제 재현한 경우만 수정한다. 읽기 전용 리뷰와 부모의 브라우저 재현을 분리했다.

대안 검토: 링크마다 클릭 방어를 추가하면 브라우저 뒤로/앞으로·프로그램 이동을 놓친다. 히스토리 임의 패치보다 설치된 React Router의 정식 useBlocker와 DataRouter를 사용하고, 탭 종료/새로고침은 beforeunload로 분리한다. 입력 데이터 자체는 브라우저 영구 저장소에 넣지 않는다. 기존 흰색·Microsoft 스타일과 업무 권한은 유지한다. 검증은 동일한 이탈 시나리오의 취소/확정, 저장 후 해제, 뒤로/앞으로, 모바일, 실제 서버 실패 및 전체 회귀로 한다.

재현 및 변경: 120자 공고명을 표시한 320px 화면의 문서 너비가 1,823px였다. 공고 카드 최소 트랙과 긴 텍스트 줄바꿈을 수정한 뒤 같은 화면의 문서 너비는 305px(스크롤바 제외)이고 상세·지원 화면에도 가로 넘침이 없었다. 1,024px 계약서 반폭 임금카드에서 금액 입력칸이 39.8px였으며, 해당 편집 카드만 전체 폭으로 바꾼 뒤 131.9px였다. 모바일 메뉴 이동 후 초점은 기존 BODY에서 main으로 이동한다. 확인 취소 시 메뉴는 열린 채 해당 링크로 초점이 복귀한다.

중간 검증: 탐색 보호·지원서 입력·레이아웃 5파일 39개 테스트 통과(0.91초), 로컬 빌드 387모듈/API 101라우트 성공, 린트 오류 0/기존 경고 3개. 실제 브라우저에서 계약 조건 이탈 취소/폐기, 지원서 Back 차단, 지원서 수정 Forward 차단, 메뉴 이동 취소/확정, 공고 임시저장 후 이탈 해제를 실행했다. 서버 400 동의 검증 오류 후 자기소개가 그대로 남았고, 동의 보완 저장→다른 페이지→돌아와 재조회한 내용도 유지됐다. 같은 지원서 다시 열기는 이제 폐기 확인이 뜬다. 불확정 응답/첨부 재전송 방지는 별도 4파일 56개 회귀 테스트로 확인했으며 브라우저 네트워크 장애 주입은 하지 않았다.

후속 확인: 독립 리뷰에서 계약 조건 저장 중 추가 입력이 성공 응답 때 덮이는 동일 종류의 입력 유실을 찾았다. 요청 스냅샷 기준 저장 판정과 지연 응답 회귀를 추가한 뒤 전체 검사를 진행한다. 아직 2회차 최종 로컬 반영·임시 데이터 정리 전이다.

추가 재현: 1,440px 채용 관리의 임시저장 표는 526px 카드 안에서 너비 900px였고 불러오기 버튼의 x좌표가 1,577px로 화면 밖이었다. 게시 공고와 같은 줄바꿈 카드 구조로 통일했다. 전체 검사 1차는 123파일 중 122파일 통과/계약 저장 재현 테스트 2개 실패(1,458개 통과, 43.33초)였으며 해당 실패를 수정 중이다. 초기 로그인 상태 조회의 guest→복원 계정 변경도 공개 입력을 초기화한다는 리뷰를 받아, 최초 복원과 실제 계정 변경을 구분하는 회귀를 함께 추가한다. 이는 미저장 입력 보호의 동일 문제이며 새 기능 확대가 아니다.

최종 변경: 계약 저장은 제출 시점 스냅샷만 저장된 것으로 판정하고 요청 중 추가 입력은 보존한다. 최초 로그인 복원은 공개 폼을 재생성하지 않으며 이후 실제 계정 전환·로그아웃은 별도 sessionEpoch로 초기화한다. 임시저장 카드의 열기 버튼은 같은 1,440px 화면에서 x=912.8~987.8px 안에 들어왔다. 실제 계약 조건 저장→공고 이동→Back→재조회에서 기본급 2,500,000원과 업무 내용이 보존됐고 저장 후 이탈 경고는 해제됐다.

최종 검증: `npm test` 124파일/1,480개 통과(29.06초), `npm run lint` 오류 0/기존 경고 3개, 일반 빌드·오프라인 빌드 모두 성공(387모듈/API 101라우트), `git diff --check` 오류 없음. 지연 계약 저장/최초 로그인 복원은 실제 컴포넌트 제어 테스트로 검증했고 브라우저 지연 응답 주입은 하지 않았다. 운영 서비스·실제 메일·외부 장치 통화는 검증하지 않았다.

로컬 반영 및 정리: 5173 서버를 최신 빌드로 재실행하고 홈 화면에 접속했다. 5189 검증 서버와 탭을 종료하고 이 회차의 일회성 가상 DB·첨부·메일, 서버 헬퍼, 배포 임시 번들을 삭제했다. 삭제한 검증 데이터는 복구용 보관하지 않았으며 운영 데이터 및 기본 5173 로컬 DB는 보존했다. 다음은 목록 조회 실패·알 수 없는 경로·이동 시 스크롤 상태를 실제로 재현한 뒤 필요한 항목만 선정한다.

## 3회차 — 필수 개선 (2026-09-19, 로컬 반영)

주간 한도 잔여 40%, 일반 사용 가능을 확인했고 초기화 크레딧은 사용하지 않았다. 5173 health에서 외부 요청 false·로컬 메일 전용·101라우트와 공고 0개를 확인했다. 404 기준선은 잘못된 주소에서 main 텍스트 0자·h1 0개였다. 짧은 페이지로 이동할 때는 브라우저가 스크롤을 0으로 줄이므로 모든 이동의 스크롤 문제를 주장하지 않는다.

선정: (1) 공고·지원서·대시보드 조회 실패와 빈 목록 구분 및 재조회, (2) 서버 저장 성공과 후속 목록 조회 실패의 분리, (3) 알 수 없는 주소의 복귀 안내, (4) 긴 페이지 간 새 탐색/Back 스크롤과 기존 키보드 초점의 공존을 재현 후 개선. 앞의 두 문제는 Promise.all 일괄 실패/토스트만 표시/성공 통지를 refresh 뒤에 두는 코드에서 확인했다. 일회성 5189 서버에 가상 데이터와 통제된 503 응답을 사용하며 운영이나 실제 메일은 호출하지 않는다.

대안: 전체 재로드는 미저장 폼을 잃으므로 실패한 자료만 다시 읽는 버튼을 제공한다. 기존 목록을 오류 없이 최신인 것처럼 유지하지 않고 성공한 영역은 유지·실패 영역은 별도 표시한다. 저장 요청을 자동 재전송하지 않으며 성공이 확정된 경우 저장 성공을 먼저 표시하고 목록만 재시도한다. 미등록 주소는 자동 홈 이동보다 오류와 복귀 링크를 보여준다. 스크롤은 사설 history 패치보다 라우터 공식 수단을 우선 검토하고 비동기 콘텐츠 높이를 실제 확인한다.

실제 기준선: 503인 공고 목록에는 '조회 장애'와 '모집 중인 공고 없음'이 동시에 보였다. 공고 POST 후 후속 GET만 실패시켰을 때 입력이 초기화되고 오류만 표시됐으나 실제 공개 목록에는 저장 공고 1건이 있었다. 1,440px 기술 페이지 y=548에서 채용 관리로 새로 이동하면 y=548이 유지되고 제목의 top=-511.5px로 화면 밖이었다. 목록별 오류·GET 전용 재조회, 저장 성공 독립 통지, 404 화면과 라우터 스크롤 복원을 구현 중이다. 같은 유형의 공고 상세/지원서 초기 조회 실패도 조사했으며 지원서 실패가 폼·첨부 입력 DOM을 제거하지 않도록 보완했다.

중간 검사: 조회 복구 테스트 1차 4파일 중 3파일 통과/선택자 3개 실패(필수 * 표시를 테스트가 누락); 선택자 보완 후 관련 5파일63개 통과. 지원서 조회 지연 실패 후 첨부 보존·모집 종료 시 제출 금지를 더해 6파일76개 통과. 린트 오류0/기존 경고3개. 이 수치는 로컬 테스트 결과이며 실제 사용자 업무 시간 절약은 측정하지 않았다.

실제 결과: 조회 실패 시 공고·지원서·대시보드는 빈 목록으로 단정하지 않는다. 공고 작성 중 GET 재시도 후 입력 유지, 공고 POST 성공/후속 GET 실패 후 성공 통지 및 GET만 재시도, 면접방 생성 성공/조회 실패 후 재조회한 방 1개를 확인했다. 지원서 최초 조회 실패에서도 이름·폼·이탈 보호가 남고 제출은 차단됐다. 잘못된 주소에서 안내와 복귀 링크→공고 목록 이동, 새 탐색 y0과 Back 위치 복원을 실행했다. 390px에서 메뉴 이동 후 닫힘·main 초점·문서 너비375px를 확인했다.

후속 수정: 공식 ScrollRestoration만으로는 비동기 계약서의 짧은 로딩 화면에서 Back 위치가 y0으로 잘렸다. POP일 때만 콘텐츠 높이를 관찰하는 보완기를 추가해 실제 계약서 y4046→기술 페이지 y0→Back y4046을 확인했다. 자동화 semantic click의 scrollIntoView에서 생긴106px 차이는 fresh screenshot 기반 실제 포인터 클릭으로 분리했고 제품 오류로 기록하지 않는다. 사용자 조작·경로·계정 변경 시 대기를 취소하고 observer/listener를 해제한다. React Router의 UNSAFE context 의존성을 한 컴포넌트에 제한했으며 업그레이드 시 계약 테스트가 필요하다.

최종 검사: 전체129파일/1541개 통과(31.91초), 린트 오류0/기존 Fast Refresh 경고3개, 일반·로컬 빌드390모듈/API101라우트 성공, diff 공백 오류 없음. 비동기 복원 사용자 중단·StrictMode·조회 경합·저장 후 갱신 실패는 제어 테스트, 목록 복구·404·계약서 Back은 실제 격리 로컬 서버/브라우저로 확인했다. 운영·외부 메일·장치 통화는 검증하지 않았다.

정리: 5189 검증 서버 종료 후 가상 공고2개·면접방1개가 포함된 일회성 DB/메일/첨부 디렉터리와 서버 헬퍼를 제거했다. 운영 및 기본5173 데이터는 보존했고, 임시 viewport를 원복했다. 5173 서버는 최신 빌드로 다시 시작해 접속을 확인한다.

로컬 반영: 최신5173 서버의 health/API 및 브라우저 채용 공고 화면을 확인했다. 공고0개, 외부 요청false, 로컬 메일 전용이 유지됐다. 다음 회차 전 일반 사용 가능·주간 잔여33%를 조회했다. 초기화 크레딧은 사용하지 않았다.

## 4회차 — 필수 개선 (2026-09-19, 로컬 반영)

후보는 쓰기 성공 뒤 응답이 유실되었을 때 공고·면접방 생성 재시도로 생기는 중복과 남은 핵심 화면의 조회 실패/복구 흐름이다. 기존 operation ID/보호가 있는 지원서·메일을 중복 구현하지 않고 생성 경로와 남은 화면만 조사한다. 실제 재현 후 최대5개 중 필요한 항목만 선정한다.

기준선 재현: 실제 로컬 PostgreSQL과 현재 handler에 같은 operationId/body를 두 번 전달하자 공고201/201·생성2개, 면접방201/201·생성2개였다. 별도 메모리 JSX 실행에서 관리자 감사 로그 GET만 실패하면 계정 생성 성공 후 새 사용자/일회성 안내가 모두 숨고 성공토스트0·오류1이었다. 문서 최초 GET 실패는 파일 없음2곳·재시도0개, upload성공/후속GET실패는 성공토스트0이었다. 테스트용DB는 메모리에서 닫았고 운영 호출은 없었다.

선정4개: 공고 생성 재시도 중복 방지, 면접방 생성 재시도 중복 방지, 관리자 부분 조회/생성 결과 복구, 문서관리 조회/쓰기후 갱신 분리. 같은 제목 자체를 중복금지하면 정상적인 별도채용을 막으므로 요청별 식별자와 소유자 범위를 사용한다. 실패한 쓰기를 무조건 자동재전송하지 않고 미확정 상태에서 같은 요청의 명시적 재시도를 제공한다. 관리자·문서의 GET 재시도는 기존입력과 확정결과를 보존한다.

중간 구현: 공고·면접방은 탭 메모리에 제출 스냅샷/UUID를 유지하고 응답 미확정 동안 입력을 잠근 뒤 같은 요청 재시도를 제공한다. 서버는 소유자/종류/UUID별 트랜잭션 잠금과 생성 원장을 사용한다. 원장은 본문·초대코드를 복제하지 않고 해시/자원ID만 보관하며 자원 삭제 뒤에도 재생성을 막는다. operationId 없는 구형 요청·화면을 떠나 키가 사라진 새 요청은 보호 범위 밖이다. PostgreSQL 다중 연결 운영 부하는 아직 검증하지 않았다.

중간 검증: 서버 PostgreSQL·보안·마이그레이션 3파일74개 통과(7.48초), UI/요청 훅 3파일46개 통과(0.73초), 실제HTTP 10개 통과(5.60초). HTTP 첫 검사에서 테스트가 기존 trial 응답의 user.id를 잘못 참조해1개 실패했고 실제 id 구조로 수정했다. 최초201/같은요청200복구/내용변경409, 공고와방 각1개를 확인했다. 로컬 빌드391모듈/API101라우트 성공. 5189 가상계정2개를 가진 격리서버에서 브라우저 검증 중이다.

실제 화면 검증: POST가 실제로 저장된 뒤 첫응답만503으로 대체했다. 공고·면접방 모두 입력잠금/명시적 같은요청재시도→정상결과 복구를 확인했으며 DB는 공고1개·방1개·원장2개였다. 관리자는 감사로그만 실패해도 사용자2명/방1개를 유지했고, 새 가상계정 생성 후 사용자/감사조회만 실패해도 일회성 안내1개는 표시됐다. GET재시도 뒤 사용자3명과 안내가 함께 유지됐고 비밀값은 출력·기록하지 않았다. 서류 최초조회실패는 파일없음으로 단정하지 않고 업로드를 막았다. PDF업로드확정/GET실패에서도 성공안내와 재조회가 남았으며 GET재시도 후 문서1개 표시, 해당 가상파일 삭제 후 빈목록을 확인했다. 화면에서 발견한 업로드 안내 조사 표기는 자연스러운 문장으로 수정했다.

최종 검사: 전체133파일/1630개 통과(32.02초), 최종 하네스/문구 관련3파일52개 통과(0.70초), 린트 오류0/기존경고3개, 일반·로컬 빌드391모듈/API101라우트 성공, diff 공백오류0. 첫 린트의 테스트하네스 명명 오류1개는 수정했다. 관리자 잘못된200응답은 사용자/방/감사/계약/채팅 목록별 복구로 처리하며 제어 회귀로 확인했다. 새 빌드에서 PDF업로드 문구와1280px 문서너비1265px·업로드 다시활성화를 확인했다.

로컬 반영/정리: 5173 서버를 최신빌드와 새 생성원장 마이그레이션으로 재시작했다. 가상계정3개·공고1개·방1개·원장2개·문서1개가 있던5189 격리DB/파일/메일, 서버헬퍼2개, 가상PDF와 빌드 임시번들을 삭제했다. 이는 일회성검증데이터이며 복구용으로 보관하지 않았다. 운영 및 기본5173 데이터는 보존했다. 아직 운영배포·실제메일·다중연결부하·장치통화는 검증하지 않았다.

5173 health/API/브라우저 접속 확인: 외부요청false·로컬메일전용·공고0개. 다음 회차 전 일반 사용 가능·주간잔여27%를 확인했고 초기화 크레딧은 사용하지 않았다.

## 5회차 — 필수 개선 (2026-09-19, 로컬 반영)

이미 검증한 디자인·입력보호·생성재시도를 반복 구현하지 않고 면접방/계약서의 남은 핵심 오류 복구·입력 유실 경로를 조사한다. 실제 재현 가능한 문제를 먼저 찾고 최대5개 이내에서 선정한다. 면접방/계약서 읽기 전용 조사와 부모의 화면·공개 폼 분석을 병렬로 분리했다.

메시지 기준선: 실제 컴포넌트 제어테스트6개가 실패했다. 면접채팅/쪽지는 전송중 새로 쓴 초안이 성공응답에서 빈문자열로 지워졌고, 같은tick 제출2회가 POST2회를 만들었다. 쪽지의 이전GET이 POST뒤에 도착하면 확정된 메시지가 화면에서 사라졌으며 상대변경 시 창에 key가 없어 이전 대화상태를 재사용했다. 방간 경합 가설은 App의 pathname key가 이미 방어하므로 제외하고 같은방/같은창 경합만 실제 문제로 분류한다. 초안은 영구저장하지 않고 제출snapshot과 편집version으로 후속입력을 보존하며 동기잠금·상대별창분리·메시지ID병합을 적용한다.

선정5개: 면접방 조회복구/최신응답보장, 면접방 쓰기확정/재조회실패 분리, 계약서 조회복구/최신응답보장, 계약서 저장확정/재조회실패 분리, 메시지 입력·확정표시 보존. 면접방 기준선은 정상1개 통과·예상실패6개, 계약서는 최초조회실패·저장후GET실패·같은방 이전GET의 최신입력덮기3개를 재현했다. 이전회차의 서버조회와 자식폼보존 패턴을 이어가며 추가업무 기능은 만들지 않는다. 새로고침을 복구수단으로 요구하는 대신 GET만 재시도하고, 쓰기 성공이 확정되면 재전송하지 않는다. 메시지6개 기준선은 수정후 일반검사6개 통과(15:45,0.42초). 아직 실제화면/전체회귀는 미검증이다.

중간 실제검증: 5189 격리서버에서 메시지 POST 응답을 지연하고 다음문장을 입력했다. 채팅과쪽지 모두 응답후 새초안이 남고 확정메시지가 표시됐으며 실제DB는 각1개였다. 메시지/기존쪽지권한 등4파일34개 통과(0.77초), 린트오류0/기존경고3개. PostgreSQL bigint가 문자열인 응답도 허용·정확정렬한다. 실제화면에서 채팅polling GET500을 발견했고, 동일 실제handler/PGlite에서 nullable SQL의 독립 $3 타입추론실패42P18을 확인했다. 일반/화상세션 모두 영향이 있어 메시지 항목의 필수수정으로 포함한다. 테스트 전용 CAST만으로 정상GET200·관리자 세션403을 확인했으며 실제소스 수정후 다시 검증한다.

통합검사1차: 전체137파일 중135통과·2실패(1732개통과/1실패/10건hook중단),70.62초. 실패는 localRuntime beforeAll25초와 실제 백업복원30초 제한이며, 기능결함 여부는 순차 재실행으로 분리 중이다. 메시지SQL 실제수정후 권한/세션검사3파일33개 통과했고, 화면에서 최초면접방 GET503→재시도 복구는 확인했다. 전형종료 브라우저 native confirm에서는 제어도구가 응답하지 않아 해당 실제화면 경로는 미확인으로 남겼다. 이를 앱 자체 오류로 단정하지 않는다.

추가 실제검증: 계약 PATCH 성공 직후 GET503에서 성공/조회실패가 각각 표시됐고 저장·서명·PDF를 잠갔다. 다음 초안을 입력한 뒤 GET만 재시도해도 초안이 보존되고 저장 버튼이 다시 활성화됐다. 실제 계약조건1개가 저장됐다. 면접방은 조건협의 메시지 저장/후속GET503에서 메시지와 초대메일 초안이 남고 추가쓰기만 잠겼다. 재조회 후 협의기록1개와 초안이 유지됐다. 별도 가상지원자의 메시지도 기존 화면에 자동 수신1회·오류없음을 확인했다. 1280px 화면의 채팅/분석은 x264/855인2열, 문서너비1265px였다.

남은 회귀: 일시적 조회잠금을 InterviewSessionPanel의 보관/종료용 disabled에 전달해 일정폼이 숨고 잘못된 종료안내를 표시했다. 일시잠금과 실제방상태를 분리해 입력DOM/선택값을 보존하도록 수정 중이다. 순차 재검사53개 중52통과·백업복원1개30초초과(96.61초)라 단순동시부하로 확정하지 않았고 실제 병목단계를 조사한다. 전체최종검사·로컬서버갱신·5회차임시자료정리는 아직 전이다.

확인창 후속: 멈춘 임시탭9는 문서화된 기본탭 API로 닫고 탭목록에서 제거를 확인했다. 다른 검증탭10의 전형종료 클릭은 도구 시간초과를 반환했지만 실제 요청은 저장되어 있었다. GET재시도로 종료상태/사유를 확인했으며 중복종료는 실행하지 않았다. 이어 전형다시진행→실제저장/GET503에서는 재개성공과 조회실패가 별도표시되고 재개버튼이 잠기는 것을 확인했다. 확인창 조작 자체의 도구응답은 안정적으로 검증하지 못했다.

최종화 중: 일정 임시잠금 회귀 포함4파일103개 통과(1.24초). 실제 화면에서 제목과 일정 날짜2개가 GET503 중 유지/잠금되고 GET복구 후 같은값으로 활성화됨을 확인했다. 잘못된 보관/종료 안내는 없어졌다. 자동화 fill만으로 날짜가 React상태에 반영되지 않는 도구동작은 실제키 입력으로 분리했다. 채팅 문자열ID 10/9의 잘못된순서·숫자/문자열중복·잘못된poll응답의 커서훼손12개를 재현해 정밀비교와 응답검증을 추가했다. PostgreSQL BIGINT 범위커서는 문자열로바인딩한다. 관련7파일90개 통과(6.19초). 초대형 ID가 로컬PGlite JS bigint로 반환될 때 공통JSON직렬화가 지원하지 않는 경계는 아직 남아 있다.

전체2차138파일/1768개 중1767통과·1실패(68.38초)는 기존면접GET 테스트의 바인딩 기대값20 대 '20' 차이였다. 새 정확문자열계약에 맞춰 기대값을 수정하고 전체를 다시실행한다. 백업복원은 원래30초제한 유지한 재실행53/53통과(14.48초), 직접시간초과원인은 미확정이다. 복원소스는 변경하지 않고 동일바이트검증을 유지하는 Buffer.equals로300KB 첨부검사비용만 줄였다. 린트오류0/기존경고3개, 일반빌드393모듈/API101라우트 성공.

최종회귀: 전체138파일/1768개 모두통과(16:16:52 시작,63.89초), diff공백오류0. 임시5189 서버와 검증탭9/10 종료, 가상계정2·면접방1·채팅5·쪽지1·계약조건1이 담긴 일회성 디렉터리, 서버헬퍼2개·빌드임시번들을 제거했다. 복구용으로 보관하지 않으며 운영 및 기본5173 데이터는 보존했다. 최신5173 로컬서버를 다시 시작해 최종접속을 확인한다. 운영·실제메일·장치통화는 이번에도 미검증이다.

로컬반영: 최신393모듈/API101라우트 빌드로5173 시작, health의 외부요청false·로컬메일전용·마이그레이션10개와 공개공고0개를 확인했다. 실제브라우저 채용공고 접속확인. 다음회차 전 일반사용가능·주간잔여16%이며 초기화크레딧은 사용하지 않았다.

## 6회차 — 필수 개선 (2026-09-19, 로컬 반영 중)

이미 검증한 상위페이지 오류복구를 반복 구현하지 않고 하위이메일/면접일정 폼과 알림목록의 미확정 저장·초안보존·조용한 조회실패를 조사한다. 이전회차에서 남은 실제PGlite 초대형ID JSON경계도 재현하되 현실적 영향과 변경위험을 비교한다. 근거있는 필수항목만 최대5개로 선정하고 구현한다. 외부메일·화상장치·클라우드 접근은 하지 않는다.

선정5개: 면접일정 목록 조회 복구, 면접일정/참여자 쓰기의 확정 상태·다음 초안 보존, 이메일 폼 중복 제출·불확정 결과 처리, 알림/쪽지함 조회 오류·응답 경합, 큰 메시지ID JSON응답 경계. 승인된 넓은 전체배치는 이미 이전 회차에서 적용·검증되어 재설계하지 않는다. 단순전체새로고침/자동재전송 대신 기존 입력·확정결과 유지와 GET전용복구를 사용한다.

기준선: 알림/쪽지함 실제컴포넌트4개 검사 모두실패(16:22:03); 최초GET실패를 빈알림으로 표시하고 늦은GET이 최신목록을 덮었다. 면접13개는 정상대조1개통과·예상실패12개재현(16:26:45). 이메일 최초6개실패는 수정후통과했으나 확장9개 중3개가 렌더전중복제출/과거실패안내로 남아 수정중이다. BIGINT 실제메모리PostgreSQL/로컬·Supabase어댑터7개실패는 공통JSON의 bigint만10진문자열변환후 신규9개·관련총85개통과(7.01초). 일반숫자/문자열/응답헤더는 보존했다. 외부통신은 하지 않았다.

통합 전 검증: 면접/알림 관련4파일118개통과(16:35:17,0.82초), 이메일/방회귀2파일86개통과(16:27:30,0.67초). 독립 리뷰에서 알림 읽음저장중 도착한 새알림까지 읽음처리하는 경합과, 느린 쪽지GET이20초poll마다 무효화되는 문제를 추가로 재현했다. 쓰기중 일반조회 차단과 진행중인 배경조회 중복생략으로 수정했고 관련15개 회귀에 포함했다. 명시적 재조회는 최신요청 우선이다. 로컬빌드393모듈/API101라우트 성공.

실제 화면: 알림/쪽지함 최초503에서 빈목록으로 단정하지 않고 오류·조회버튼을 표시했다. 알림 읽음POST 뒤 GET503은 읽음결과와 오류를 함께 유지했다. 면접 생성POST201 뒤 상세GET503에서도 생성된제목·성공안내가 남고 추가쓰기를 잠갔다. GET복구 후 같은일정1개가 표시됐다. 시간등록은180일범위 밖 요청400을 정상거절했고, 허용기간 등록성공/목록GET503에서는 성공안내·추가등록잠금을 표시했다. GET만 재시도 후 시간1개와 활성화된 입력을 확인했다.

이메일 확인창 조작은 브라우저 도구가 응답하지 않아 안정적으로 검증하지 못했다. 닫힌 검증탭의 대기중 클릭이 뒤늦게 처리되어 실제 로컬 발송1건이 저장된 것을 확인했고 재전송하지 않았다. 재조회 결과 status=sent·attempts=1·로컬메일1개, 새 화면에는 서비스접수와 수신함도착 미확인이 구분됐다. 외부발송0건이며 요청미확정/다음초안/중복방지는 컴포넌트 제어테스트로 검증했다. 전체테스트와 최종정리는 진행중이다.

최종검사: 전체143파일/1852개 통과(16:44:04 시작,54.19초), 린트오류0/기존FastRefresh경고3개, 일반빌드393모듈/API101라우트 성공, diff공백오류0. 5189검증서버 종료 후 가상계정2·면접방1·면접일정1·선택시간1·쪽지1·메일1이 포함된 일회성DB/메일디렉터리와 헬퍼2개·빌드임시번들을 삭제했다. 복구용으로 보관하지 않았으며 운영 및 기본5173 DB는 보존했다. 임시탭12를 닫고5173 최신빌드로 재시작한다.

로컬반영: 최신5173에서 외부통신false·로컬메일전용·101라우트·마이그레이션10개와 채용공고 화면접속을 확인했다. 다음회차 전 주간잔여6%·일반사용가능. 초기화크레딧은 사용하지 않았다.

## 7회차 — 필수 개선 (2026-09-19, 로컬 반영 중)

선정후보1개는 면접 시간 등록·예약 응답계약의 남은 오류다. SlotPicker.run이 모든 성공HTTP반환을 저장확정으로 처리하고 GET의 available/duration/recording 필드를 검증하지 않는 코드근거가 있다. 실제서버의 등록{id}/철회{withdrawn:true}/예약·변경{session}계약과 비교해 잘못된응답 재현 후 수정한다. 넓은배치·기존업무흐름·API권한은 바꾸지 않고 불확정일 때 초안보존/GET재조회만 제공한다. 서버가 반환하는 정상값을 불필요하게 거부하지 않는지 대조검사하며 별도격리UI에서 저장후응답훼손을 검증한다.

실제기준선: 5189격리DB의 정상슬롯은30분·녹화필수·예약가능1건이었다. GET200에서{id,startsAt}만남기자 실제UI가 소요시간숫자없이 '분 · 녹화 없음 · 예약 불가'로 표시했다. 원본DB는변경하지않았다. POST실제저장후응답을{}200으로바꾸어도 성공통지와초안삭제가일어났다. 새회귀는 실제서버 정상응답대조와 쓰기/조회필드 오류를 비교한다.

기준선테스트70개 중 정상대조40통과·예상오류30개실패(16:52:10,0.76초). 동작별실제응답검증과 슬롯필수값검증을 넣은 뒤 관련3파일138개통과(16:53:34,0.87초), 범위린트오류0. 정상false불리언·허용소요시간6개는유지한다. 쓰기응답불명확시 이전확정목록/입력보존·GET복구로통일했다. 예약응답은 방·선택슬롯·일시·소요시간·녹화조건도 비교한다.

사용자레이아웃질문에따라현재홈을다시확인했다. 실제1280px화면에서 왼쪽탐색232px/본문1033px,상단2열494.9px+321.7px,하단2열472.5px씩이었다. 문서너비1265px로가로넘침없음을화면과DOM으로확인했다. 운영사이트에는미배포다.

수정후실제UI: 불완전GET에서도기존정상30분/녹화필수/예약가능을보존하고오류·쓰기잠금을표시했다. 저장후{}응답은성공으로단정하지않고입력2026-10-22T10:00을보존했다. GET만재시도후새슬롯을확인했으며POST누계2·슬롯2로중복재전송은없었다. 가상지원자로로그인해10월21일10시예약→22일10시변경을실행했으며같은면접ID로갱신·기존시간다시선택가능을확인했다. 정상서버예약/변경응답도수용한다. 철회native확인창은이전회차도구한계로이번UI에서반복하지않았고제어테스트로만확인했다.

최종검사: 전체143파일/1887개통과(16:55:03 시작,61.69초), 린트오류0/기존경고3개, 일반·로컬빌드393모듈/API101라우트성공·diff공백오류0. 이어진실행환경에서기존서버handle이사라져실제Node프로세스목록도조회했고5173/5189해당서버가없음을확인했다. 중복실행하지않고검증임시탭13·가상계정2/방1/슬롯2/면접1포함디렉터리·헬퍼2·배포번들을정리했다. 기본로컬DB와운영은보존. 다음회차전주간잔여4%·일반사용가능이며초기화크레딧은사용하지않았다.

7회차 최신로컬서버를다시실행하고health의외부요청false/로컬메일전용/API101과공고0개를확인했다.

## 8회차 — 필수 개선, 로컬 반영 완료 (2026-09-20)

계정복구 화면의 실제상태표시·중복요청을점검한다. 후보는 (1) 재설정/인증메일요청중 입력변경·중복제출·늦은결과, (2) 비밀번호설정·인증완료화면의 잘못된성공응답/이탈후완료처리다. 기존중앙집중개선배치는유지한다. 실제응답계약과재현테스트로검증된항목만수정하며서버보안정책·수신자존재여부비공개는유지한다. 외부서비스/실제메일은호출하지않는다.

2026-09-20 재개: 사용자가 현재까지 작업을 마무리하고 배포하도록 지시하여 새 회차는 시작하지 않고 8회차 검증과 로컬 반영까지만 수행한다. 앞서 지정한 로컬 전용 범위를 유지하며 GitHub 푸시·운영 배포는 하지 않는다. 사용량 한도 오류로 하위 작업이 중단되었으나 새 조회는 일반 사용 가능·주간 사용 0%였고 초기화 크레딧은 사용하지 않았다.

기준선: 메일 요청 27개 중 24개 실패·3개 정상 대조 통과(17:07:27), 계정 변경 완료 10개 모두 실패(17:06:53). 같은 순간 두 번 POST, 잘못된 200 응답의 완료 처리, 늦은 상태 변경, 비밀번호 변경 성공 뒤 세션 조회 실패의 혼동을 재현했다. 실제 격리 브라우저에서도 이메일 수정 뒤 이전 요청 안내가 남았다. 입력 초안 비교보다 요청 중 필드 잠금과 동기 제출 ref를 선택하여 비밀번호 소거 경합과 대상 혼동을 함께 막는다. 서버의 일반 응답은 계정 존재·메일 도착·주소 정정 완료를 보장하지 않으므로 안내 의미는 유지한다. 검증은 임시 5189 환경에서만 수행하며 기본 로컬 DB는 보존한다.

변경: 복구메일 요청 2화면의 동기 제출 잠금·필드 잠금·이전 안내 초기화·응답 계약 확인, 비밀번호 재설정/변경/가입인증 3화면의 완료 계약·수명 검사, 인증 Context/API의 잘못된 계정 게시·저장 차단을 적용했다. 비밀번호 변경 POST가 확정된 후 세션 조회만 실패하면 성공 사실은 유지하고 GET만 재시도한다. 인증 페이지의 라우트 재생성 전에 증명 정리·이동 콜백을 실행하고 화면 이탈 시 AbortSignal을 API 응답 수락 전까지 전달한다. 새 기능이나 추가 회차는 시작하지 않았다.

검증: 요청 UI 27/27, 완료 UI 33/33, Context/세션 저장 104/104, 서버/로컬/요청 관련 53/53 통과. 전체 첫 실행은 1974개 중1973통과·1실패였으며 기존 공개폼 테스트의 축약 계정을 실제 인증 계약으로 수정했다. 재실행 145파일/1974개 모두통과(2026-09-20 15:00:43 시작,32.43초). 일반·로컬 빌드394모듈/API101라우트 성공, oxlint오류0·기존FastRefresh경고3, diff공백오류0. 개별 eslint 호출은 프로젝트 설정파일이 없어 실행 불가였고 지정된 npm run lint(oxlint)로 검사했다.

실제 UI: 수정 전 {} 응답은 아무 안내 없이 끝났고 주소 편집 뒤 과거 접수 안내가 남았다. 최신 빌드에서는 불완전 응답을 접수 불확실 안내로 표시하고, 요청 중 이메일·버튼을 잠그며, 실제 일반202 응답 후 주소를 바꾸면 이전 안내를 지웠다. 지연 요청은 카운터3→4로1번만 증가했다. 정상 기본 DB·운영 데이터·실제 메일은 변경하지 않았다. 비밀번호 변경 및 인증 완료의 실제 브라우저 제출은 수행하지 않았고 컴포넌트·API·PostgreSQL 검사로 검증했다. 운영 통합/외부 메일/실제 통화 장치는 미검증이다.

정리: 5189 검증서버와 이전5173 서버 종료를 실제프로세스목록으로 확인하고, 임시5189 탭·격리DB/로컬메일 디렉터리·헬퍼2개·빌드검사용번들2개를 삭제했다. 복구용 복제는 남기지 않았고 기존 기본로컬DB와 기존 사용자파일은 보존했다. 최신5173 서버 시작 및 최종 접속을 확인한다.

최종 반영 확인: npm run local로394모듈/API101라우트를 다시 빌드하고 http://127.0.0.1:5173 에 실행했다. health는 environment=local·externalRequests=false·emailDelivery=local-mailbox-only·현재마이그레이션10개였다. pg_cron/pg_net/Vault를 사용하는 운영 보존기간 예약은 로컬환경 특성상 실행하지 않는다. 공개공고 API는 postings=[]이며, 초기확인에서 jobs 필드로 집계한 숫자1은 잘못된집계여서 원문계약확인후공고0개로정정했다. 실제브라우저에서 넓은 홈배치·공고0개표시·로그인·재설정페이지 이동을 확인했고 홈 탭을 사용자 결과로 남겼다. 새 기능회차는 시작하지 않고 이번 로컬 마무리 요청을 종료한다. GitHub푸시·Cloudflare/Supabase운영배포·실제메일발송은 실행하지 않았다.

## 운영 릴리스 — 2026-09-20 반영 확인

사용자가 별도로 운영 사이트 배포와 GitHub 푸시를 명시 승인했다. 대상은 k95691368-ship-it/portfolio master, Supabase obumqkwkvnemkyaahjbn, Cloudflare portfolio(portfolio-epa.pages.dev)다. 기본 CLI 계정 목록 때문에 초기 접근을 잘못 판단했으나 프로젝트를 명시한 Supabase 조회와 Cloudflare 기존 인증 프로필로 접근을 확인했다. 다른 프로젝트는 수정하지 않는다.

배포 전 전체145파일/1974테스트·일반빌드·로컬빌드 통과 결과와 정적검사 오류0/기존경고3을 확인했다. 변경 파일198개에 비공개 키 패턴 검사 결과 발견0개였다(완전한 비밀정보 부재 보장은 아님). 독립 검토에서 새 마이그레이션4개는 삭제/자동발송 없이 추가형이며 민감 테이블 RLS와 공개권한 회수를 포함한다. 적용 순서는 001~004 → API → 프런트엔드다. 최신 retention 함수는 예약 정리 범위를 변경하므로 이번에는 배포하지 않고 기존 버전을 유지한다. 실제 이메일·지원자 데이터 생성·기존 데이터 삭제는 수행하지 않는다. 오류 발생 시 이전 프런트/API 버전을 복구하되 추가 테이블과 사용자 데이터는 삭제하지 않는다.

운영 서버 반영: 202609190001~004 마이그레이션4개 적용 후 로컬/운영 이력10개 일치를 확인했다. API v23 ACTIVE, retention v6 유지. 전후 집계는 계정1·공고1·지원서0·면접방0·임시저장0·서명계약0으로 동일했다. 새 민감 테이블5개 모두 RLS=true, anon/authenticated SELECT=false. 실제 운영 GET은 /jobs=200(공고1), /me=200(user:null), /postings·/application-self-service·/admin/users=401 및 운영사이트 CORS/no-store를 확인했다. 프런트엔드 배포와 GitHub 푸시는 이어서 검증한다.

코드 커밋95401296a89b9610f4ae49540fdd8ea6afb4cef6을 master에 푸시했고 GitHub Actions 35494075352의 의존성 설치·린트·시험·빌드가 모두 success였다. Cloudflare Git 연동이 동일 커밋을 운영 배포45c5602d-edcb-4c7d-833f-240b17f9adcd로 반영했다. 운영 도메인의 엔트리 app-CVDq8rK8.js는 검증한 dist와 일치했고 연결 JS/CSS/폰트7개 모두200이었다. 홈·공고·로그인·가입인증·비밀번호재설정요청·비밀번호재설정·지원관리 URL7개가 새 HTML을 반환했고, 마지막4개 민감경로는 no-referrer/no-store였다.

실제 운영 브라우저에서 왼쪽 탐색/넓은2열 홈 배치, 기존 공고1개 표시, 로그인 유지 체크박스, 비밀번호 재설정 화면으로의 링크 이동을 확인했다. 로그인 제출·실제메일·지원/계약 생성·통화·새 retention 예약처리는 이번 운영 검증에서 실행하지 않았다. 검증용 키 패턴 검사 임시파일은 삭제했고 기존 데이터와 다른 프로젝트는 보존했다. 이 마지막 검증 기록도 별도 커밋으로 푸시한다.
